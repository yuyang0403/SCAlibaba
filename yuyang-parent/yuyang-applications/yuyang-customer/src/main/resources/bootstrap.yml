server:
    port: 41111
    servlet:
        context-path: /customer
management:
    endpoints:
        web:
            exposure:
                include: '*'
spring:
    application:
        name: yuyang-customer
    cloud:
        nacos:
            config:
                group: applications
                file-extension: yml
    profiles:
        active: dev